# Configuration Reference — Strategist Skill

**Status:** Accepted
**Last Updated:** 2026-06-26

All configuration files live in `.strategist/` inside the installed repository. Manual edits require recompilation (`strategist compile`) for the agent to use the updated version.

---

## Directory Structure

```
.strategist/
  active.yaml                  Main skill configuration
  personas/
    pragmatic.yaml             Analytical persona
    epic.yaml                  Strategic persona
  roles/
    default.yaml               Slot providers (generated by install)
    mission.yaml               Example mission configuration
    spec-driven.yaml           Configuration for spec-driven flow
  knowledge.index.yaml         Knowledge sources by task_type
  memory/
    source-hints.yaml          Learned priority adjustments (learning loop)
  .compiled/                   Compiled artifacts (do not edit manually)
    .config.gz
    .domain.gz
    .index.gz
    .manifest.gz
```

---

## active.yaml

Central file that defines the operating mode and configuration binding.

**Schema:**

```yaml
mode: pragmatic | epic          # Active persona. Required.
base_path: .analysis            # Directory where mission artifacts are written.
roles_config: roles/default.yaml  # Relative path to the roles file (within .strategist/).
knowledge_index_path: knowledge.index.yaml  # Knowledge index path (relative to .strategist/).

language:                       # Language configuration (optional).
  ui: pt-BR                     # User interface language.
  docs: en                      # Documentation artifact language.
  chat: pt-BR                   # Agent chat message language.
  code: en                      # Code artifact language.

slots:                          # Explicit provider binding per slot (required).
  discovery: brainstorming
  refinement: openspec-explore
  execution: sniper

# treasure_chests:              # Offline knowledge sources (optional).
# - id: <identifier>
#   path: <path>
#   scope: all | discovery | refinement | execution
```

**Fields:**

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `mode` | string | yes | — | Active persona. Accepts `pragmatic` or `epic`. |
| `base_path` | string | no | `.analysis` | Root where `pending/`, `refined/`, and `archived/` are created. |
| `roles_config` | string | yes | `roles/default.yaml` | Relative path to the roles file within `.strategist/`. |
| `knowledge_index_path` | string | no | `knowledge.index.yaml` | Knowledge index path relative to `.strategist/`. |
| `language` | object | no | — | Language configuration: `ui`, `docs`, `chat`, `code`. |
| `slots` | object | yes | — | Provider binding: `discovery`, `refinement`, `execution`. |
| `treasure_chests` | array | no | — | Offline knowledge sources with `id`, `path`, and `scope`. |

**Example generated by install (pragmatic mode):**

```yaml
mode: pragmatic
base_path: .analysis
roles_config: roles/default.yaml
knowledge_index_path: knowledge.index.yaml

slots:
  discovery: brainstorming
  refinement: openspec-explore
  execution: sniper
```

The `mode` can be overridden per mission via `--mode=epic` without modifying this file.

The `slots:` defines the explicit provider binding and is required. It is equivalent to declaring providers in `roles_config`, but takes precedence when both are present.

The `treasure_chests` field is optional. Each entry requires `id`, `path`, and `scope`. The `all` scope passes the chest to all slots; specific scopes (`discovery`, `refinement`, `execution`) restrict which slots receive the chest.

---

## roles/*.yaml

Defines which providers are used in each pipeline slot. Install generates `roles/default.yaml`.

**Schema:**

```yaml
discovery: <provider_id>       # Ranger slot provider. risk_score must be write_analysis.
refinement: <provider_id>      # Archivist slot provider. risk_score must be write_analysis.
execution: <provider_id>       # Sniper slot provider. risk_score must be controlled.
```

**Example — roles/default.yaml:**

```yaml
# Slot keys: discovery, refinement, execution
# Internal names: Ranger, Archivist, Sniper (epic) / analysis, refinement, execution (pragmatic)
discovery: brainstorming
refinement: openspec-explore
execution: sniper
```

**Provider resolution:** the Strategist looks for `<provider_id>/skill.yaml` in the configured paths. If not found, stops with `slot_provider_not_found`.

**risk_score validation:** each slot has a required risk_score. Mismatches stop the pipeline with `slot_risk_mismatch`.

| Slot | Required risk_score |
|------|---------------------|
| `discovery` | `write_analysis` |
| `refinement` | `write_analysis` |
| `execution` | `controlled` |

**Available roles:**

| File | Usage |
|------|-------|
| `default.yaml` | Configuration generated by install (`brainstorming` + `openspec-explore` + `sniper`) |
| `mission.yaml` | Configuration optimized for high-complexity missions |
| `spec-driven.yaml` | Specification-driven flow (useful with SDD) |

---

## personas/*.yaml

Defines the agent's tone, vocabulary, and message templates. The persona does not change the pipeline — only the language and style of outputs.

**Schema:**

```yaml
id: pragmatic | epic

phase_labels:                   # Labels displayed in progress messages
  discovery: <label>
  refinement: <label>
  execution: <label>

tone_directive: >               # Tone instruction for the agent
  <free text>

progress_prefix: "[Strategist]" # Fixed prefix in all progress events

prompt_templates:               # Templates interpolated at runtime
  intake_summary: >
    <template with {task_type}, {delivery_strategy}, ...>
  phase_start: >
    <template with {phase_label}, {provider}, {n}, {total}>
  phase_done: >
    <template with {phase_label}, {artifact_path}>
  approval_prompt: >
    <template with {artifact_path}>
  analysis_delivered_result: >
    <template with {artifact_path}>
```

**Differences between personas:**

| Aspect | pragmatic | epic |
|--------|-----------|------|
| Discovery phase | `analysis` | `Ranger` |
| Refinement phase | `refinement` | `Archivist` |
| Execution phase | `execution` | `Sniper` |
| Tone | Analytical, direct | Strategic, authoritative |
| Ambiguities | "open questions" | "risks to resolve" |

---

## knowledge.index.yaml

Defines knowledge sources queried by `context-enrichment` before each mission. Filtered by `task_type`.

**Schema:**

```yaml
schema_version: "1"
description: >
  <description>

sources:
  - id: <unique identifier>
    type: docs | examples | directives
    path: <absolute or repository-relative path>
    tags: [<task_type>, ...]    # "all" loads the source for any task_type
    priority: high | medium | low
```

**Source fields:**

| Field | Type | Description |
|-------|------|-------------|
| `id` | string | Unique identifier. Referenced in `source-hints.yaml`. |
| `type` | string | `docs` (documentation), `examples` (examples), `directives` (directives) |
| `path` | string | Path to a file or directory. Directories are read recursively. |
| `tags` | string[] | `task_type` values that activate this source. Use `all` to always include. |
| `priority` | string | Loading priority. Adjustable via learning loop. |

**Recognized task_types:**

| task_type | When used |
|-----------|----------|
| `architecture_analysis` | System architecture analysis |
| `refactor` | Code refactoring |
| `general` | Fallback when no pattern matches |

**Example:**

```yaml
sources:
  - id: project-architecture
    type: docs
    path: /abs/path/to/docs/architecture
    tags: [architecture_analysis, architecture]
    priority: high

  - id: team-directives
    type: directives
    path: /abs/path/to/team-directives.md
    tags: [all]
    priority: high

  - id: past-good-examples
    type: examples
    path: .analysis/.strategist/patterns/good
    tags: [examples, refactor]
    priority: medium
```

An empty `knowledge.index.yaml` (no `sources`) is valid — the pipeline continues without context enrichment.

---

## memory/source-hints.yaml

Priority adjustments learned by `learning-curator` after previous missions. Generated and managed automatically — do not edit manually.

**Schema:**

```yaml
- source_id: <source id in knowledge.index.yaml>
  annotation: <curator observation>
  priority_adjustment: +1 | -1 | 0
  task_type: <task_type of the mission that generated this hint>
  recorded_at: <ISO 8601 timestamp>
```

These hints are applied on top of the base priority declared in `knowledge.index.yaml` before each mission.

---

## Mission Parameters (runtime flags)

Passed directly when invoking the Strategist, without modifying configuration files:

| Parameter | Type | Description |
|-----------|------|-------------|
| `mode` | `pragmatic \| epic` | Overrides `active.yaml.mode` for this mission only |
| `roles` | string | Name of the roles file to use (default: `default`) |

---

## Validation

Use `strategist validate` to verify the configuration before running a mission:

```bash
strategist validate                     # validates .strategist/ in the current directory
strategist validate --root=/other/path  # validates an alternative root
```

See [cli-reference.md](cli-reference.md) for details on reported errors.
