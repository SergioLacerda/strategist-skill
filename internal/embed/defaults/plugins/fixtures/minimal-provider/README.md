# Minimal provider contract fixture

This offline fixture exercises the package/adapter relationship used by
`strategist provider validate`. It is contract evidence only: it contains no
runtime invocation implementation and therefore cannot certify live provider
execution.
