# Strategist project adapter (ADR-0033 two-layer contract) for the portable
# openspec-explore/SKILL.md package in this same directory.
#
# canonical_role: ranger — corrected from archivist (mission
# 20260914-openspec-explore-canonical-role-correction,
# docs/adr/0036-openspec-explore-canonical-role-correction.md). The prior
# assignment was made from a Strategist-authored description, before the
# real upstream package content (now vendored verbatim in SKILL.md) was
# available: the real package is an explore/investigate/think-before-changing
# workflow, which is discovery-shaped, not refinement-shaped.
canonical_role: ranger
roles:
  - ranger
risk_score: write_analysis
category: discovery
# default: false — brainstorming remains the sole permanent discovery
# default (DEC-001, docs/adr/0035-embedded-weapon-fallback-policy.md).
# openspec-explore is a secondary, opt-in discovery weapon.
default: false

# Upstream provenance (ADR-0029 DEC-002), evidence recorded in
# docs/adr/0029-external-skill-provider-lifecycle.md's own Context section
# at the time this ADR was accepted (2026-08-20) — transcribed here, not
# re-researched. This is this package's own provenance; it is not
# Archivist's tool (see docs/adr/0036-openspec-explore-canonical-role-correction.md
# — openspec-propose is, and this package's canonical_role above was
# already corrected to ranger).
upstream_repo: Fission-AI/OpenSpec
upstream_skill_path: skills/openspec-explore/SKILL.md
upstream_version: "1.10.0"
upstream_commit: 1ebddd17f40dde15dfd28289e4493c3cf05ee9df
upstream_content_digest: sha256:7e0281fc4f094dcced0c5469e7cbceba2dce9f113f22a4e34efcdce4315edf81
# license: not recorded in ADR-0029 — left unpopulated rather than guessed.

# supported_handoff_schemas is intentionally omitted: the fixed Ranger role
# owns normalization into its handoff schema.
