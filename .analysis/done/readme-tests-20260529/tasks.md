# Tasks — Seção de Testes no readme.md
**Mission ID:** readme-tests-20260529

---

## Tarefa única: editar readme.md

Inserir a seção `## 🧪 Testes` imediatamente antes de `## 📄 Licença`.

A seção deve conter:

1. Pré-requisitos por runner (tabela ou blocos por grupo)
2. Comandos de instalação dos pré-requisitos (macOS/Debian/Arch)
3. Como rodar o runner de fixtures (`run-tests.sh`)
4. Como rodar o runner de unit/integration (`harness/run-tests.sh` ou `make test`)
5. Como rodar componentes individualmente (validators, unit, integration)
6. Nota sobre BDD specs (documentação, sem runner)

**Escopo:** modifica `readme.md` na raiz do projeto (fora de `.analysis/`).
