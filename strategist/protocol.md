# Strategist — Protocol

> [!NOTE]
> This file has been merged into `templates/agent-protocol.md` (GAP-ST-06).
> The compiler now generates a single, unified protocol document.

Runtime copies live under `.strategist/`; mission artifacts resolve from `base_path`.

Do not simulate role work. If a configured provider cannot be invoked, stop with
`role_invocation_failed`.

## Response Contract

See `templates/agent-protocol.md`.

## Compliance Summary

See `templates/agent-protocol.md`.

## Mission Result

See `templates/agent-protocol.md`.
