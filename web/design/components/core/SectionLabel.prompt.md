Ornamental section header (wide-tracked uppercase Cinzel label over a "⟡ ━━━ ⟡" rune rule) — use to open each region of a page.

```jsx
<SectionLabel>mission pipeline</SectionLabel>
<SectionLabel rule={false} align="left">roles / party</SectionLabel>
```

Props: `rule` (toggle the divider), `align`.
