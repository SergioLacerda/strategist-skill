The inviolable approval-gate **portal** — stone jambs flank an arched doorway closed by an iron portcullis, topped by a keystone, with a glowing central lock above the requirement headline + supporting copy. Use to mark a hard human-approval checkpoint or any "stop, confirmation required" moment.

```jsx
<GatePanel heading="REQUIRES :: human confirmation · no exceptions">
  Discovery and refinement run autonomously. Execution proceeds only after human approval.
</GatePanel>
```

Props: `lock` (center glyph, default ⛨), `heading`.
